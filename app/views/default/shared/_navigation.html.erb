<nav class="bg-white border-b border-gray-200">
  <div class="container mx-auto px-6 py-3">
    <div class="flex items-center justify-between">
      <!-- Logo placeholder -->
      <div class="flex-shrink-0">
        <a href="/" class="text-xl font-bold">Your Name</a>
      </div>
      <!-- Navigation items -->
      <div class="flex items-center space-x-4">
        <% if Spina::Navigation.find_by(name: 'main') %>
          <% Spina::Navigation.find_by(name: 'main').navigation_items.sorted.live.each do |nav_item| %>
            <div>
              <%= link_to nav_item.page.title.upcase, nav_item.materialized_path, class: "hover:text-gray-600 hover:text-gray-900" %>
            </div>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</nav>